/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn3d.iconscout.com", 
      "cdn.iconscout.com",
      "img.icons8.com",
      "i.pravatar.cc"
    ],
  },
};

module.exports = nextConfig;
